# for installing environment you should add All privilege to user
# [example] --> examlpe group

[localhost]
localhost ansible_connection=local

[web_hosts]
pc_server ansible_ssh_user="{{ ubuntu_user }}" ansible_ssh_pass="{{ pass }}" ansible_ssh_port="{{ ansible_ssh_port}}" ansible_ssh_host="{{ ansible_ssh_host}}"
